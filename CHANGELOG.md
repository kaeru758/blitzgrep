# Changelog

## 0.6.0

会話ログの形式が変わっても黙って壊れないようにし、CI と英語 README を整えた。

- **`BlitzGrep: 会話ログを診断` を追加。** transcript の形式は Claude Code の内部仕様で公開契約ではない。
  変わったときに「一致はありません」としか出ないのが最悪なので、
  何本読んで・何レコードから・どのブロックを取り出せたかを数えて報告する。
  知らないブロック種別も一覧に出す。そのまま不具合報告に貼れる形式。
- **検索が黙って 0 件を返さないようにした。** 会話ログを読めたのに全ファイルから 1 件も
  取り出せなかった場合、「一致なし」ではなく形式変化の可能性としてエラーに出す。
- **CI を追加** (`.github/workflows/ci.yml`)。Ubuntu / macOS / Windows の 3 OS で
  型チェック・単体・webview・統合の全スイートを回し、vsix が作れることも確認する。
  - CI には VS Code が無いため、同梱 ripgrep を探せず単体テストが黙って skip される問題があった。
    落とした VS Code から appRoot を割り出して指させ、**CI では skip を失敗として扱う**ようにした。
    「3 OS で緑」が本当に検証された緑であることを保証するため。
  - `.gitattributes` で改行を LF に固定。Windows で書いたものが CRLF で入ると、
    他の OS との差分がコードの変更と混ざって読めなくなる。
- **英語 README を追加** (`README.en.md`)。日本語版と相互リンク。

## 0.5.0

「一致そのもの」を指すようにし、この拡張機能が何をするものかを見せるようにした。

- **ピンポイントのハイライト。** 行の目印と一致の目印が同じ琥珀色で、一致が行に沈んでいた。
  役割を分けた。一致そのものは**不透明に塗り潰して文字色まで変える**（蛍光ペン）。
  行は無彩色の薄い地 + 行頭の帯 + gutter の ▶ に退かせ、位置を示すだけにした。
  - 装飾は生成順がそのまま重ね順になる。行全体の地が一致を上塗りしていたので、
    生成順を `all → 行 → 点滅 → 一致` に固定した。
  - 色を追加: `blitzgrep.currentMatchBackground` / `blitzgrep.currentMatchForeground`。
    `blitzgrep.currentLineBackground` の既定を無彩色に、`currentMatchBorder` を落ち着いた枠色に変更。
- **出所の印を全ての見出しに揃えた。** 📄 いま / 🌿 ブランチ / ◆ 履歴 / 📍 blame / 💬 会話ログ を
  同じ位置・同じ幅で出す。出所追跡で 4 種類が混ざった一覧が一目で読める。
- **検索前に「4 つの出所」を見せるようにした。** 1 つの語を 4 つの出所へ投げるのがこの拡張機能の
  骨格なので、何もしていない時間にそれを出す。
- README に**他の拡張機能との違い**を追加。GitLens とは答える問いが違う
  （git が知っていること vs. 会話にしか残っていない「なぜ」）ことと、やらないことを明記した。

## 0.4.1

会話ログのヒットを開くと、文書の先頭 (`# 会話ログ`) が光ってしまう不具合を直した。

- **通し番号の食い違いを直した。** 検索は「ツール結果」設定で発言に番号を振るのに、
  ビューアは「クリックしたヒット自身が tool_result か」で組み立てていた。
  ツール結果を有効にして検索すると、それ以外のヒットで番号がズレて 0 行目に落ちていた。
  番号を振った条件を出所 (`withToolResults`) に持たせ、同じ並びで開くようにした。
- **本文中の `## ` を発言の見出しと数えないようにした。** 会話には Markdown の見出しが普通に出てくる。
  話者マーク (`## 👤` / `## 🤖`) まで込みで見出しを判定する。
- **外したときの落ち方を変えた。** 位置を特定できなければ、文書全体から検索語を探して
  「その語が実際にある行」へ着地する。それでも見つからなければ 0 行目ではなく発言の先頭へ寄せ、
  ステータスバーで特定できなかったことを伝える。

## 0.4.0

待ち時間と操作を説明するようにした。

- **パイプラインを可視化した。** 「出所を追う」は blame → 履歴 → 会話ログ の 3 段で走るのに、
  途中は「検索中…」としか出ていなかった。走る前に 3 段すべてを並べて出し、
  各段が 待機 → 実行中 → 完了 と進むのをレールで繋いで見せる。段ごとにヒット件数も出す。
  - 走らない段も理由つきで残す（「まだコミットされていません」「エディタの行が起点にないため」など）。
  - 空振りしたときの自動再検索を段の注記に出す（`"foo" で再検索中` → `"foo" で再検索`）。
  - ブランチ横断検索は `7/20 ブランチ` と進捗バーを出す。
  - 中断すると、走らずに終わった段が畳まれる。
- **対象の説明を出すようにした。** 5 つの対象は名前だけでは区別が付かないので、
  選んだものが何をするのかを 1 行で出す。
- **空のときに次の一手を出すようにした。** 「一致はありません。」で終わらせず、
  対象ごとの手立てを並べる（会話ログなら全プロジェクト / ツール結果、履歴なら `--all` と上限、など）。
  検索前は `Ctrl+Alt+O` などの入口を案内する。
- webview のスモークテストを追加（`test/webview.js`）。最小の DOM 上で `media/main.js` を走らせ、
  パイプラインと結果一覧の描画を検証する。VS Code の中でしか動かない部分の穴を埋めるため。

## 0.3.1

「どこを開いたのか分からない」を潰した。

- **開いた行が見える。** 結果から飛んだ行に、行全体の背景・行頭の帯・gutter の ▶ マーカーを付け、
  行の中の一致は太枠で囲むようにした。開いた直後は一瞬光る。
  会話ログの Markdown のように一致位置を特定できず範囲が幅ゼロになる場合でも、
  行全体の目印は必ず出るので見失わない。
  - 設定 `blitzgrep.highlightCurrentLine` / `blitzgrep.flashOnJump` で切れる。
  - 色は `blitzgrep.currentLineBackground` / `blitzgrep.currentMatchBorder` /
    `blitzgrep.flashBackground` としてテーマから上書きできる。
  - `blitzgrep.highlightInEditor` は「他の一致すべて」だけを担当するようになった。
- **結果一覧の階層が見える。** ヒット行を見出しの下に字下げして縦のガイドで束ね、
  見出しには背景とグループの区切り線を入れた。
- **いま何を見ているか見える。** スクロールで流れ去った見出しを一覧の上端に貼り付けるようにした。
  クリックするとその見出しまで戻る。
- 選択行がフォーカスを失っても左の帯で残るようにした（エディタ側を触っている間も見失わない）。
- 結果一覧で `←` / `→` によるグループの折りたたみ / 展開に対応。操作のヒントを一覧の下に表示する。

## 0.3.0

3 つのソースを 1 コマンドに繋いだ。

- **`BlitzGrep: この実装はどこから来た？`** (`Ctrl+Alt+O`) を追加。
  カーソル行を起点に、次の 3 つを 1 回で引いて 1 つの結果リストに並べる。
  1. `git blame` — その行を最後に触ったコミット (📍)
  2. `git log -S` — その文字列が現れた / 消えたコミット (◆)
  3. Claude Code の会話ログ — それについて話していた発言 (💬)
- 行そのもので空振りしたときは、行から特徴語を 1 つ抽出して自動で再検索する
  (会話の地の文にコード 1 行がそのまま出ることは稀なため)。何で引き直したかは結果に表示する。
  日本語は漢字列・カタカナ列だけを内容語として取り出す (助詞を含む塊は検索語にならないため)。
- サイドバーの対象に「出所を追う」を追加。エディタから起動しなくても、
  検索ボックスの語について履歴 + 会話ログをまとめて引ける。

## 0.2.0

「いつ入った / なぜ入った」を追えるようにした。

- **履歴検索 (pickaxe)** を追加。`git log -S` / `-G` を `--patch --unified=0` で流し読みし、
  検索語を含む追加行 (`+`) / 削除行 (`-`) だけをコミット単位にまとめて表示する。
  結果を開くと、追加行はそのコミットの版、削除行は親の版 (消える直前) が読める。
- **Claude Code 会話ログ検索**を追加。`~/.claude/projects/**/*.jsonl` を横断し、
  発言・思考・ツール引数・ツール結果をブロック単位で検索する。
  結果は会話を読める Markdown に組み立て直して開き、該当発言まで飛ぶ。
  - セッション UUID のサブディレクトリにあるサブエージェントの会話も対象。
  - 既定は現在のワークスペースに対応するプロジェクトのみ。全プロジェクト横断も可能。
  - 抽出結果をファイル単位でキャッシュ (mtime + size で検証)。
- 結果一覧をコミット単位 / セッション単位でグループ化するように変更。
- `blitzgrep.clearCaches` コマンドと `blitzgrep.historyMaxCommits` 設定を追加。
- ripgrep のロケータが `@vscode/ripgrep-universal` レイアウトに対応し、
  未知のレイアウトにも `@vscode` 配下の走査で追従するようになった。

## 0.1.0

初回リリース。

- ripgrep によるワーキングツリーのインクリメンタル全文検索（ストリーミング表示・前の検索は即キャンセル）
- `git grep` によるローカル / リモート追跡ブランチの横断検索（並列実行・ブランチ間の重複行を集約）
- GitHub 仮想リポジトリ (`vscode-vfs://github/...`) のブランチ検索（tarball を 1 リクエストで取得してメモリ上で検索、コミット SHA 単位でキャッシュ）
- 結果一覧とエディタ内の両方でのハイライト、`F4` / `Shift+F4` でのジャンプ
- チェックアウトしていないブランチのファイルを読み取り専用タブで開く機能
- 仮想リストによる 1 万件規模の結果表示
